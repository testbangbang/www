<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data class="ItemPersonalTaskBinding">

        <variable
            name="bean"
            type="com.onyx.jdread.personal.model.PersonalTaskData" />

        <import type="com.onyx.jdread.util.Utils"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{bean.taskName}"
            android:textColor="@android:color/black"
            android:textSize="@dimen/level_two_heading_font" />

        <TextView
            android:id="@+id/task_receive"
            android:layout_width="@dimen/item_personal_task_receive_width"
            android:layout_height="wrap_content"
            android:background="@drawable/text_selector"
            android:enabled="@{Utils.isEquals(bean.taskStatus, @string/allow_get)}"
            android:gravity="center"
            android:padding="@dimen/item_personal_task_receive_padding"
            android:text="@{bean.taskStatus}"
            android:textColor="@android:color/black"
            android:textSize="@dimen/level_two_heading_font" />
    </LinearLayout>
</layout>
